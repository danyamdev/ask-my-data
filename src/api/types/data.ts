export type TFile = {
  columns: string;
  datetime: string;
  description: string;
  file_extension: string;
  filename: string;
  headers: string;
  invalid: string;
  missing: string;
  outliers: string;
  rows: string;
  target: string;
};
